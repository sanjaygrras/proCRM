import { ActionButtonDirective } from './action-button.directive';

describe('ActionButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionButtonDirective();
    expect(directive).toBeTruthy();
  });
});
